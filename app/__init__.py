"""架构-执行双模型编排器（Orchestrator）。

核心设计沿用 Aider 的 architect / editor 两段式：

1. **架构段（GPT）**：把需求转成结构化纲领（目标、原则、组件、步骤、验收标准）。
2. **执行段（DeepSeek V4）**：只按纲领逐步落地，产出文件变更与命令建议。

本包只做编排与落地，不直接连接券商/交易系统，也不执行任何未经确认的命令。
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
